version: "2"
sql:
  - engine: "postgresql"
    schema: "dbos/schema.sql"
    queries: "dbos/queries"
    gen:
      go:
        package: "db"
        out: "dbos/internal/db"
        sql_package: "pgx/v5"
        emit_pointers_for_null_types: true
        emit_prepared_queries: false
        emit_interface: false
        emit_exact_table_names: false
        emit_empty_slices: true
